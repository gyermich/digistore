<h3>Cart</h3>
<table class="table">
    <thead>
    <tr>
      <th></th>
      <th></th>
      <th>Qty</th>
      <th>Subtotal</th>

    </tr>
  </thead>
  <tbody>
  {{# each orderitems }}
  <tr>
    <td><img {{ bind-attr src=item.image }} class='tiny_image'/></td>
    <td>{{ item.name }}</td>
    <td>
      {{ quantity }}
      <button {{ action 'more' this }}>+</button>
      <button {{ action 'less' this }}>-</button>
    </td>
    <td>${{ subtotal item.price quantity }}</td>
  </tr>
  {{/ each}}
  <tr>
    <td><h4>Total:</h4></td>
    <td></td>
    <td></td>
    <td>{{ total }}</td>
  </tr>
</tbody>
</table>
